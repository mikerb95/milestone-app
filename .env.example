# --- Turso ---
# turso db create milestone && turso db show milestone --url
TURSO_DATABASE_URL="libsql://milestone-xxxx.turso.io"
# turso db tokens create milestone
TURSO_AUTH_TOKEN=""

# --- Auth.js ---
# openssl rand -base64 32
AUTH_SECRET=""
# En Vercel no hace falta: se deduce del despliegue.
AUTH_URL="http://localhost:3000"

# --- Google OAuth (opcional) ---
# https://console.cloud.google.com/apis/credentials
# Redirect URI: {AUTH_URL}/api/auth/callback/google
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
