import { GoalsList } from "@/components/goals/goals-list";

/* Con el detalle abierto, el slot principal sigue mostrando la lista. */
export default function GoalsDefault() {
  return <GoalsList />;
}
